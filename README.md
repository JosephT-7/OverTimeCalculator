Simple overtime calculator app to start relearning Java since its been over half a decade since I programmed. 
Relearned basic Java syntax, method creation, simple (clean) code structure, and debugging.

Using Itellij IDEA. I will probably make a simple GUI for this as well since I've never learned to do that before. 
